<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.vainsolutions.myfirstparseproject.MainActivity">

    <EditText
        android:id="@+id/titleTxv"
        android:layout_width="match_parent"
        android:layout_height="55dp"
        android:hint="Title" />

    <Button
        android:id="@+id/submitBtn"
        android:layout_width="match_parent"
        android:layout_height="55dp"
        android:layout_below="@id/titleTxv"
        android:text="Submit" />

    <Button
        android:id="@+id/refreshListBt"
        android:layout_width="match_parent"
        android:layout_height="55dp"
        android:layout_below="@id/submitBtn"
        android:text="Refresh List" />


    <ListView
        android:id="@+id/arrayLv"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/refreshListBt"></ListView>

</RelativeLayout>
